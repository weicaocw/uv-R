# uvr 课程地图（CURRICULUM）

> 方法：自顶向下**理解**全貌，自底向上**建造**。教学课在 `docs/lessons/`，每步一篇；提交 / PR 中英双语。
> **当前总目标**：把 uvr 补齐到"能与 pak 等工具做**公平的端到端 benchmark**"——为此需要 多仓库 → 缓存(暖缓存) → binary 包(免编译) → 并行。

## 模块总览
| 章 | 模块 | 状态 |
|---|---|---|
| A 版本模型 / B 元数据 / C 联网 / D 求解(教学版) / E 安装 / F CLI / G Benchmark / D′ pubgrub | 见下 | ✅（v0.1–v0.3，PR #1–#8） |
| **H 多仓库** | 合并多仓库索引、跨仓库求解与安装、多 `--repo` | ✅ → v0.4 |
| I 缓存 | PACKAGES / tarball 本地缓存（暖缓存，冷 640ms→暖 6.8ms） | ✅ → v0.5 |
| J binary 包 | 优先用预编译 binary（免编译，公平对比安装） | ⬜ 计划 |
| K 并行 | 并行下载 / 安装 | ⬜ 计划 |
| L 端到端 benchmark | 补齐后，对 pak 做**全流程**公平对照 + 报告 | ⬜ 目标 |

## 各模块小步索引（详见 docs/lessons/，共 25 课）
- **A** 01–07 · **B** 08–11 · **D** 12–15 · **F** 16 · **C** 17–18 · **E** 19–20 · **G** 21 · **D′** 22–23。
- **H 多仓库** — 24 索引合并 + 来源追踪 · 25 CLI 多 `--repo` + 跨仓库求解/安装。

## 已守住的底线
- E 安装到**项目本地 R 库**（`-l` 隔离，未碰全局 R）。
- G **自写计时脚本**（不装 hyperfine）；只比能公平比的轴，重活诚实报"打平"。

## 通往"敢和 pak 比"的路线
1. ✅ H 多仓库 — 求解贴近真实世界。
2. I 缓存 — 暖缓存场景（uv 招牌轴）。
3. J binary 包 — 安装免编译，与 pak 同条件。
4. K 并行 — 吞吐对齐。
5. L 端到端 benchmark — 全流程对照 pak，出诚实报告。
